export const metadata = {
  title: "Quản trị nội dung KIGEN",
  description: "Sanity Studio — quản lý sản phẩm KIGEN Life Sciences.",
  robots: { index: false, follow: false },
};

export default function StudioLayout({ children }) {
  return children;
}
